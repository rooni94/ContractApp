# 📄 ContractApp – JavaFX Document Auto-Generation Tool

ContractApp هو تطبيق JavaFX بسيط يُستخدم لإدخال بيانات العامل/الكفيل، ثم يقوم تلقائيًا بتحميل قالب عقد Word (العقد.doc او الفيزا.doc أو الهوية الوطنية ID)، واستبدال الـ Bookmarks بداخله، وتوليد ملف عقد جديد (c2.doc) جاهز للطباعة وذلك باستخدام مكتبة **Spire.Doc for Java**.

---

## 🚀 المميزات

- واجهة JavaFX كاملة بدون FXML (بناء الواجهة داخل ContractGUI.java).
- إدخال جميع بيانات العامل والكفيل عبر حقول (TextFields – DatePickers – ComboBoxes).
- توليد عقد Word جديد تلقائيًا عبر استبدال Bookmarks داخل ملف القالب.
- استخدام Spire.Doc لمعالجة ملفات Word بسهولة.
- إمكانية تشغيل المشروع من أي جهاز بمجرد وضع المكتبات في مجلد `lib/`.
- مسار القالب والإخراج أصبح نسبيًا (recommended) ليعمل على أي بيئة تشغيل بدون تعديل.

---

## 📂 هيكلة المشروع

```
ContractApp/
│
├── src/
│   └── sample/
│       ├── ContractGUI.java
│       ├── Contract.java
│       ├── Contracts/
│       │   ├── c1.doc
│       │   └── c2.doc
│       ├── sample.fxml (غير مستخدم)
│       └── Controller.java (غير مستخدم)
│
├── lib/
│   ├── spire.doc.jar
│   ├── javafx-controls.jar
│   ├── javafx-graphics.jar
│   └── javafx-base.jar
│
├── out/
└── README.md
```

---

## 🔧 المتطلبات

### ✔ JDK 8–17  
### ✔ Spire.Doc for Java  
### ✔ JavaFX SDK (إن كنت تستخدم JDK 11+)

---

## ▶️ التشغيل من Terminal

### Compile:
```
javac -cp "lib/*;src" --module-path lib --add-modules javafx.controls,javafx.graphics ^
      -d out src/sample/*.java
```

### Run:
```
java -cp "lib/*;out" --module-path lib --add-modules javafx.controls,javafx.graphics sample.ContractGUI
```

---

## 🧪 اختبار التطبيق

1. فتح التطبيق  
2. ملء كل الحقول  
3. الضغط على Done  
4. فتح ملف c2.doc للتحقق من نجاح استبدال Bookmarks

---

## 👨‍💻 Developer

Ramazan Dev
